# Distribution notes

The npm package ships the complete R package sources, generated FFI contract,
native adapter, examples, tests, and build metadata. Platform-specific native
binaries are intentionally excluded and must be linked to libpolycall locally.
